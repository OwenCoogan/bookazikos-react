export enum Routes{
  home = "/",
  login = "/login",
  register = "/register",
  admin = "/admin",
  sendAdminInvitation = "/send-admin-invitation",
  adminInvitationValidation = "/admin-invitation-validation",
  dashboard = "/dashboard",
  profile = "/profile",
  createPost = "/create-post",
  contact = "/contact",
}
